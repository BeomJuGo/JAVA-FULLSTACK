# === Server / Timezone ===
server.port=8080
spring.jackson.time-zone=Asia/Seoul

# --- Cloudinary ---
cloudinary.cloud-name=dpuougrym
cloudinary.api-key=968227386262155
cloudinary.api-secret=BPzQcZR6dyabjKtXipZ5hPw3mzs

# 프로필 이미지 업로드 폴더(사용자/트레이너 공용)
cloudinary.folder=healthcare/uploads
cloudinary.upload-preset=Health
cloudinary.thumb-transformation=

# === Datasource (로컬 MySQL) ===
spring.datasource.url=jdbc:mysql://localhost:3306/healthcare?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=3784

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Seoul
# 개발 중 콘솔 SQL
# logging.level.org.hibernate.SQL=debug
# logging.level.org.hibernate.type.descriptor.sql=trace

# === Flyway ===
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# === Swagger (springdoc) ===
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# === JWT (예시) ===
jwt.issuer=healthcare-app
jwt.secret=change-me-use-env
jwt.access-token-validity-seconds=3600

server.error.include-message=always
server.error.include-binding-errors=always

# JWT
app.jwt.secret=change-this-super-secret-please-32bytes-min
app.jwt.exp-min=120

# === OpenAI API ===
# OpenAI API 키는 환경 변수로 설정하거나 별도 설정 파일을 사용하세요
# 예: openai.api-key=${OPENAI_API_KEY:your-api-key-here}
openai.api-key=${OPENAI_API_KEY:}
openai.api-url=https://api.openai.com/v1/chat/completions
openai.model=gpt-3.5-turbo
